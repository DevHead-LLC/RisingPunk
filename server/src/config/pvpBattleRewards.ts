/**
 * PvP (Hack Map user defender) battle rewards — source of truth for XP when the attacker wins.
 * NPC XP continues to come from each NPC's `battleExperienceReward` in the database.
 */
export const PVP_BATTLE_EXPERIENCE_REWARD = 250;
