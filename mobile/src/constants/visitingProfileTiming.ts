/** Delay (ms) before clearing visiting-profile state after closing the modal. Shared by HackMapScreen and TurfScreen so behavior stays in sync. */
export const VISITING_PROFILE_CLOSE_DELAY_MS = 350;
