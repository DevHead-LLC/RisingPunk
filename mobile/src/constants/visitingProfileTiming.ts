/** Delay (ms) before clearing visiting-profile state after closing the modal. Shared by HackMapScreen and TurfScreen. Kept at 300 to match modal close animation and avoid lag. */
export const VISITING_PROFILE_CLOSE_DELAY_MS = 300;
